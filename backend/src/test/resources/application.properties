spring.datasource.url=${TEST_JDBC_DATABASE_URL}
spring.datasource.username=${TEST_JDBC_DATABASE_USERNAME}
spring.datasource.password=${TEST_JDBC_DATABASE_PASSWORD}

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.jpa.hibernate.ddl-auto=none